## 2025-05-27 - Syntax Error in Codebase
**Learning:** Always check file syntax before assuming it works, even if it looks like a simple HTML file. In this case, a bad merge or copy-paste had broken the critical edit function.
**Action:** Run a syntax check (like `node -c` or similar) on extracted scripts if no build process exists.
