# Palette's Journal

## 2024-05-22 - Accessibility & Broken Forms
**Learning:** Sometimes "bad UX" is actually "broken code" disguised as UI. I found duplicate form fields and syntax errors in the modal logic.
**Action:** Always verify the underlying logic when seeing confusing UI behavior (like duplicate buttons or fields). Fix the foundation before applying polish.
